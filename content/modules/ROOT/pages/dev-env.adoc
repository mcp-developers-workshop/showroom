:imagesdir: ../assets/images
# Access Your Development Environment

You will use Visual Studio Code (VS Code) based on https://developers.redhat.com/products/openshift-dev-spaces/overview[Red Hat OpenShift Dev Spaces^]. **Changes to files are auto-saved every few seconds**, so you don't need to explicitly save changes.

. Navigate to https://devspaces.{ROUTE_SUBDOMAIN}#https://github.com/learn-quarkus/ibm-quarkus-beginners-workshop-m1-labs/tree/ibm-3.27[access the Red Hat OpenShift Dev Spaces instance^] and select *Log in with OpenShift* button:
+
image::login_with_openshift.png[login,800]


. When prompted, login using your IBMid
+
[NOTE]
====
In case you see the *Authorize Access* page as below, select *Allow selected permissions* button.

image::auth-access.png[auth-access, 800]
====

. The next page will display progress as devspaces is *Starting workspace quarkus-workshop*. It takes about *60* seconds to finish the process.
+
image::starting-workspace.png[ds, 800]
+

. When prompted with *Do you trust the authors of this repository?* popup, select `Do not ask again for other repositories` to continue using your workspace.
+
image::trust-author.png[ds, 800]

. If prompted with this popup, choose `Start your workspace` to access your workspace
+
image::starting-workspace-info.png[ds, 800]


. In a few seconds, you'll be placed in the workspace.
+
image::ds-workspace.png[ds, 800]
.. Click on `Trust Publishers & Install` when prompted.
+
image::ds-trust-pub-popup.png[ds, 800]

.. And also click on `Yes, I trust the authors` as indicated below.
+
image::ds-trust-popup.png[ds, 800]


. Keep this browser tab open throughout the course of this workshop. 
+
TIP: If things get weird, you can simply reload the browser tab to refresh the view.

Ready? Let's go!
